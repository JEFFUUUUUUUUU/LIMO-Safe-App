package com.example.limo_safe.utils

/**
 * Global flags for controlling application behavior
 */
object AppFlags {
    /**
     * Flag to control when biometric authentication is allowed
     * This prevents biometric prompts during splash screen
     */
    var allowBiometricAuthentication = false
}
